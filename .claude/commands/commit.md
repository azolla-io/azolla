Generate a commit message for all current uncommited changes. Te commit message should have a concise title that tells the most important change, with concise bullets for all important changes (ignore minor ones). It should NOT include any Claude promotional message. Let the user review and confirm the commit message. Commit and push once user confirmed the final commit message.
