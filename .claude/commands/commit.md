Run the following steps:
- generate a commit message for all current uncommited changes
- the commit message should have a concise title that tells the most important change, with concise bullets for all important changes
- do not include any CLaude promotional message
- let the user review and confirm the commit message
- commit and push once user confirmed the final commit message
